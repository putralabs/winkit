{
  "name": "winkit-server",
  "private": true,
  "version": "0.1.0",
  "description": "WinKit cloud processing scaffold (PRD §50) - local-first fallback, not a product",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/multipart": "^9.4.0",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
